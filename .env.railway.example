# Environment Variables for Railway Deployment
# These will be set automatically by Railway when you reference the PostgreSQL service

# Database Configuration (set these in Railway dashboard)
PGDATABASE=${{Postgres.PGDATABASE}}
PGUSER=${{Postgres.PGUSER}}
PGPASSWORD=${{Postgres.PGPASSWORD}}
PGHOST=${{Postgres.PGHOST}}
PGPORT=${{Postgres.PGPORT}}

# Django Secret Key (generate a new one for production)
SECRET_KEY=your-secret-key-here

# For local development, create a .env file with your local values:
# SECRET_KEY=your-local-secret-key
# PGDATABASE=your-local-db-name
# PGUSER=your-local-username
# PGPASSWORD=your-local-password
# PGHOST=localhost
# PGPORT=5432

